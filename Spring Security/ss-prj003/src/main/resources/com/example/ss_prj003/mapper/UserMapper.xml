<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ss_prj003.mapper.UserMapper">

    <select id="findUserByUserId" resultType="com.example.ss_prj003.domain.User">
        select uid,user_id as userId, user_pw as userPw, nickname, signup_date as signupDate, auth, refresh_token as refreshToken
            from users where user_id = #{userId}
    </select>

    <select id="findUserByUserUid" resultType="com.example.ss_prj003.domain.User">
        select uid,user_id as userId, user_pw as userPw, nickname, signup_date as signupDate, auth, refresh_token as refreshToken
            from users
            where uid = #{uid}
    </select>

    <update id="updateRefreshToken">
        UPDATE users
            SET refresh_token = #{refreshToken}
            WHERE uid = #{uid}
    </update>

</mapper>